<%= form_for(@militarystronghold) do |f| %>
  <% if @militarystronghold.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@militarystronghold.errors.count, "error") %> prohibited this militarystronghold from being saved:</h2>

      <ul>
      <% @militarystronghold.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
