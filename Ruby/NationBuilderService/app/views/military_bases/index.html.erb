<h1>Listing military_bases</h1>

<table>
  <thead>
    <tr>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @military_bases.each do |military_base| %>
      <tr>
        <td><%= link_to 'Show', military_base %></td>
        <td><%= link_to 'Edit', edit_military_base_path(military_base) %></td>
        <td><%= link_to 'Destroy', military_base, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Military base', new_military_base_path %>
