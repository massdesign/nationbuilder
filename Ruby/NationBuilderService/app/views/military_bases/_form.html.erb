<%= form_for(@military_base) do |f| %>
  <% if @military_base.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@military_base.errors.count, "error") %> prohibited this military_base from being saved:</h2>

      <ul>
      <% @military_base.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
