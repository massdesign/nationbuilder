<%= form_for(@military_basis) do |f| %>
  <% if @military_basis.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@military_basis.errors.count, "error") %> prohibited this military_basis from being saved:</h2>

      <ul>
      <% @military_basis.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
