<%= form_for(@energy_building_type) do |f| %>
  <% if @energy_building_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@energy_building_type.errors.count, "error") %> prohibited this energy_building_type from being saved:</h2>

      <ul>
      <% @energy_building_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :energySource %><br>
    <%= f.text_field :energySource %>
  </div>
  <div class="field">
    <%= f.label :powerOutput %><br>
    <%= f.number_field :powerOutput %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
