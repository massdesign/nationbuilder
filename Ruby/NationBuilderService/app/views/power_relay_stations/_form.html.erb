<%= form_for(@power_relay_station) do |f| %>
  <% if @power_relay_station.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@power_relay_station.errors.count, "error") %> prohibited this power_relay_station from being saved:</h2>

      <ul>
      <% @power_relay_station.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :capacity %><br>
    <%= f.number_field :capacity %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
